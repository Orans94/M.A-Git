//we have to ignore repository location when loading it - and automatically load it to C:\\magit-ex3
// on commit- before commit - delete empty directories from WC
// fix back button from repository page
// notification sidebar should be available on all the pages
// sort commits before sending the data to the table(on the servlet before printing the data).
// clicking upload file without choosing file - repository loaded successfully.
// on checkout - if checking out to rb - ask if he wantto create RTB
// on pr - check that the branch names that the user entered exists